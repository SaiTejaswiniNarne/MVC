@model IEnumerable<WebApplication4.Models.Degree>

@{
    ViewData["Title"] = "Index";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DegreeAbbrev)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DegreeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumberOfTerms)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DegreeAbbrev)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DegreeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumberOfTerms)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DegreeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DegreeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DegreeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
